

<div class="col-lg-3">
    <div class="left_sidebar_area">
        <aside class="left_widgets p_filter_widgets">

            <div class="l_w_title">
                <h3>Categories</h3>
            </div>
            <div class="widgets_inner">
                <div class="col col-12">
                    @await Component.InvokeAsync("SearcKategoriList")
                </div>
            </div>
        </aside>

        @*<aside class="left_widgets p_filter_widgets">
            <div class="l_w_title">
                <h3>Product filters</h3>
            </div>
            <div class="widgets_inner">
                <div class="col col-12">
                    @foreach (var item in Model)
                    {
                        <label class="checkbox"><input type="checkbox" name="checkbox"><i></i>&nbsp;@item.kategoriAdi</label><span>&nbsp;(@item.kategoriID)</span><br />
                    }
                </div>
            </div>
        </aside>

        <aside class="left_widgets p_filter_widgets">
            <div class="l_w_title">
                <h3>Color Filter</h3>
            </div>
            <div class="widgets_inner">
                <div class="col col-12">
                    @foreach (var item in Model)
                    {
                        <label class="checkbox"><input type="checkbox" name="checkbox"><i></i>&nbsp;@item.kategoriAdi</label><span>&nbsp;(@item.kategoriID)</span><br />
                    }
                </div>
            </div>
        </aside>*@
    </div>
</div>